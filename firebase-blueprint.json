{
  "entities": {
    "FeaturedBoard": {
      "title": "Featured Board",
      "description": "A saved Monopoly board theme generated by the AI.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the board."
        },
        "theme": {
          "type": "string",
          "description": "The theme name of the board."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the generated theme image."
        },
        "boardData": {
          "type": "string",
          "description": "JSON stringified board data containing spaces, players, etc."
        },
        "createdAt": {
          "type": "timestamp",
          "description": "When the board was saved."
        },
        "createdBy": {
          "type": "string",
          "description": "UID of the admin who saved it."
        }
      },
      "required": ["theme", "boardData", "createdAt", "createdBy"]
    }
  },
  "firestore": {
    "featured_boards/{boardId}": {
      "schema": "FeaturedBoard",
      "description": "Collection of featured boards saved by the admin."
    }
  }
}
